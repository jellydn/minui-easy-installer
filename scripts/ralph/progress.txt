## Codebase Patterns
- Tauri v2 with React frontend, Vite bundler
- TypeScript strict mode enabled
- Rust edition 2021, tauri 2.x
- Icons must be RGBA PNG format (not RGB)
- Pin `plist` to 1.7.0 and `serde_with` to 3.11.0 to avoid `time` crate v0.3.47+ conflicts with `cookie` 0.18.1
- Use `oxlint` for linting (already configured in package.json)
- Frontend dev server on port 1420
- Use `vitest` for TypeScript unit testing (tests in `*.test.ts` files)
- Device profiles and types live in `src/types/`

## 2026-06-13 - US-001
- Session: (local)
- Scaffolded Tauri v2 application with Rust backend and React frontend
- Created basic Home screen component with styled UI
- Configured TypeScript, ESLint (oxlint), and Rust build pipeline
- Resolved `time`/`cookie` crate dependency conflict by pinning plist and serde_with versions
- Created placeholder RGBA PNG icons for Tauri bundling
- All quality checks pass: typecheck, lint, cargo check

**Learnings for future iterations:**
- Icons must use RGBA color type (6), not RGB (2) — Tauri validates this at compile time
- `cookie` v0.18.1 conflicts with `time` v0.3.47+ due to trait coherence issues; pin `plist` to 1.7.0 and `serde_with` to 3.11.0
- `tauri-plugin-shell` pulls in `cookie` transitively — can omit if not needed initially
- `cargo check` from `src-tauri/` directory for faster iteration than `cargo build`
- Frontend: Vite dev server, React 18, TypeScript strict mode
---

## 2026-06-13 - US-002
- Session: (local)
- Created device profiles for all 8 supported handhelds
- Added TypeScript types for DeviceProfile and InstallPathRules
- Implemented getDeviceProfile() and getAllDeviceProfiles() functions
- Added vitest as test framework
- Created unit tests for device profile lookup
- All quality checks pass: typecheck, lint, vitest

**Learnings for future iterations:**
- Device profiles live in `src/types/device.ts`
- Use vitest for TypeScript testing (config in `vitest.config.ts`)
- Tests run with `npm test` or `vitest run`
---

## 2026-06-13 - US-003
- Session: (local)
- Created Rust backend command `get_removable_drives` for detecting SD cards
- macOS: uses `diskutil list external` with text parsing fallback to `df`
- Windows: uses PowerShell `Get-CimInstance Win32_LogicalDisk` where DriveType=2
- Created TypeScript types in `src/types/drive.ts` with formatSize helper
- Created DriveSelector React component with refresh button and selection UI
- Updated Home.tsx with device selector and drive selector cards
- Added CSS styles for drive list and selection states
- Created unit tests for formatSize and getDriveDisplayName
- All quality checks pass: typecheck, lint, vitest, cargo check

**Learnings for future iterations:**
- Drive detection lives in `src-tauri/src/drives.rs`
- Tauri v2 custom commands don't need explicit capability permissions (core:default suffices)
- Use `diskutil list external` on macOS for reliable removable drive detection
- Windows removable drives have DriveType=2 in Win32_LogicalDisk
- Frontend invokes Rust commands via `@tauri-apps/api/core` invoke()
- All drive fields use Option types to handle missing filesystem/size info
---
